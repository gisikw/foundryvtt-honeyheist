{
  "name": "honey-heist",
  "version": "1.0.0",
  "description": "CSS compiler for the Honey Heist system",
  "scripts": {
    "css": "gulp css",
    "watch": "gulp",
    "gulp": "gulp",
    "lint": "eslint --ext .js . --cache",
    "lint:fix": "prettier --write {.,**}/*.js && eslint --ext .js . --cache --fix"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "eslint-plugin-prettier": "^3.4.0",
    "gulp": "^4.0.2",
    "gulp-less": "^4.0.1",
    "jest": "^26.6.3"
  },
  "devDependencies": {
    "eslint": "^7.24.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "prettier": "^2.2.1"
  }
}
